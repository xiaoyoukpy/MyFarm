# 我的农场（My Farm）开发文档

## 1. 项目概述

**项目名称**：我的农场（My Farm）  
**游戏类型**：笔记本风格农场经营模拟游戏  
**核心玩法**：通过笔记本管理农场，翻页推进日期，计划自动执行结算

## 2. 技术栈

| 技术 | 用途 |
|------|------|
| HTML5 | 页面结构 |
| CSS3 | 样式、动画、纯CSS图标 |
| JavaScript (ES6+) | 游戏逻辑、数据管理 |
| localStorage | 数据持久化 |

## 3. 文件结构

```
我的农场/
├── index.html              # HTML骨架
├── css/
│   ├── base.css            # 基础样式、CSS变量、重置
│   ├── notebook.css        # 笔记本外观样式
│   ├── layout.css          # 三栏布局样式
│   ├── components.css      # 组件样式（卡片、按钮、模态框等）
│   ├── animations.css      # 动画样式（翻页动画等）
│   └── icons.css           # 纯CSS图标（天气、作物、建筑）
├── js/
│   ├── data.js             # 初始数据、配置常量
│   ├── storage.js          # StorageManager类，localStorage操作
│   ├── game.js             # Game类，核心游戏逻辑
│   ├── ui.js               # UIManager类，渲染和DOM操作
│   └── main.js             # 入口文件，初始化和事件绑定
└── 开发文档.md              # 本文档
```

### 3.1 CSS文件说明

| 文件 | 职责 | 主要内容 |
|------|------|----------|
| base.css | 基础样式 | CSS变量定义、样式重置、字体引入 |
| notebook.css | 笔记本外观 | 纸张纹理、阴影效果、复古风格 |
| layout.css | 布局结构 | 三栏布局、响应式适配 |
| components.css | UI组件 | 卡片、按钮、模态框、表单、进度条 |
| animations.css | 动画效果 | 翻页动画、过渡效果、关键帧 |
| icons.css | CSS图标 | 天气图标、作物图标、建筑图标 |

### 3.2 JS文件说明

| 文件 | 职责 | 主要内容 |
|------|------|----------|
| data.js | 数据配置 | 初始数据模板、天气概率、作物/建筑配置 |
| storage.js | 存储管理 | saveGame()、loadGame()、resetGame() |
| game.js | 游戏逻辑 | Game类、翻页结算、计划执行、作物生长 |
| ui.js | 界面渲染 | UIManager类、DOM操作、事件处理 |
| main.js | 入口初始化 | 页面加载、实例化、事件绑定 |

## 4. 数据结构

所有数据存储在 localStorage 的 `myFarmData` 键下：

```javascript
const gameData = {
  version: '1.0.0',      // 版本号，便于数据迁移
  day: 1,                 // 当前游戏日
  gold: 100,              // 金币
  maxPlans: 2,            // 最大并行计划数
  
  farm: {
    crops: [],            // 作物列表
    buildings: [],        // 建筑列表
    landCount: 2          // 田地数量
  },
  
  plans: [],              // 计划队列
  letters: [],            // 信件列表
  flags: {},              // 全局状态标记
  dailyLogs: []           // 每日统计
};
```

## 5. UI布局

### 5.1 三栏式笔记本布局

```
+------------------------------------------------------+
|  📓 我的农场    第 X 天    金币: XXX                  |
+----------------+-------------------------------------+
|  左侧目录       |   右侧内容区                         |
|  [📬 通讯]     |                                     |
|  [🌾 农场情况]  |   (根据左侧选择显示)                |
|  [📋 计划项目]  |                                     |
|  [📊 日志]     |                                     |
+----------------+-------------------------------------+
|   [📖 翻页 →] 按钮                                    |
+------------------------------------------------------+
```

### 5.2 视觉风格

- **背景色**：米黄色/奶油色 (#fcf8f0)
- **字体**：手写体或衬线字体
- **纸张纹理**：CSS渐变+阴影模拟
- **图标**：纯CSS绘制（天气、作物、建筑）

## 6. 核心功能模块

### 6.1 翻页系统

```
点击翻页 → 验证锁定 → 日期推进 → 计划执行 → 作物生长 → 事件触发 → 信件触发 → 渲染更新
```

### 6.2 计划系统

| 计划类型 | 说明 |
|----------|------|
| build | 建造建筑 |
| plant | 种植作物 |
| upgrade | 升级建筑 |
| purchase | 购买物品 |

### 6.3 信件系统

- 按天数触发
- 支持回信选项
- 引导教程信件（第1-5天）

### 6.4 作物系统

```javascript
{
  id: 'crop_1',
  name: '胡萝卜',
  plantDate: 1,
  daysToHarvest: 3,
  status: 'growing'  // growing | harvestable | empty
}
```

## 7. 游戏流程

### 7.1 初始状态

- 金币：100
- 田地：2块（已种植胡萝卜）
- 建筑：无
- 信件：第1天欢迎信

### 7.2 引导教程

| 天数 | 信件 | 引导内容 |
|------|------|----------|
| 第1天 | 欢迎来到你的农场 | 介绍翻页玩法 |
| 第2天 | 看看你的田地 | 查看作物状态 |
| 第3天 | 开始你的第一个计划 | 种植胡萝卜 |
| 第5天 | 建筑指南 | 建造2号田地 |

### 7.3 每日结算顺序

1. 验证与锁定
2. 日期推进 (day++)
3. 计划执行 (daysRemaining--)
4. 作物生长检查
5. 随机天气生成
6. 随机事件触发（10%概率）
7. 信件触发
8. 渲染更新
9. 数据保存

## 8. 天气系统

| 天气 | 概率 | CSS图标 |
|------|------|---------|
| 晴 | 50% | 圆形+光芒 |
| 雨 | 25% | 倾斜线条 |
| 阴 | 15% | 多个圆重叠 |
| 雪 | 10% | 六角星 |

## 9. 交互反馈

| 场景 | 反馈 |
|------|------|
| 翻页 | 动画→结算→更新UI→轻提示 |
| 添加计划 | 确认框→加入队列→UI更新 |
| 收获作物 | 点击收获→金币增加→收入提示 |
| 阅读信件 | 展开内容→标记已读→红点消失 |
| 操作错误 | 自定义模态框提示 |

## 10. 数据持久化

- **自动保存**：每次数据变更后调用 `saveGame()`
- **自动读档**：页面加载时检查localStorage
- **重置功能**：二次确认后清除数据

## 11. 代码组织

```javascript
class Game {
  // 核心游戏逻辑
  initGame()
  flipPage()
  executePlans()
  growCrops()
  triggerEvents()
}

class UIManager {
  // 渲染和DOM操作
  render()
  renderLetters()
  renderFarm()
  renderPlans()
}

class StorageManager {
  // localStorage读写
  saveGame()
  loadGame()
  resetGame()
}
```

## 12. 性能要求

- 交互响应时间 < 200ms
- 翻页动画帧率 ≥ 30fps
- 适配1280px以上桌面屏幕

## 13. 扩展预留

- 季节系统
- 成就系统
- 多存档功能

## 14. 交付清单

| 文件 | 说明 |
|------|------|
| index.html | HTML骨架 |
| css/base.css | 基础样式、CSS变量、重置 |
| css/notebook.css | 笔记本外观样式 |
| css/layout.css | 三栏布局样式 |
| css/components.css | 组件样式 |
| css/animations.css | 动画样式 |
| css/icons.css | 纯CSS图标 |
| js/data.js | 初始数据、配置常量 |
| js/storage.js | StorageManager类 |
| js/game.js | Game类 |
| js/ui.js | UIManager类 |
| js/main.js | 入口文件 |

---

**开发完成标准**：
1. 所有文件可正常运行
2. 翻页动画流畅
3. 数据正确持久化
4. 引导教程完整
5. 所有交互反馈正常

---

## 15. 开发顺序

按照以下顺序开发，确保每个模块独立测试：

1. **data.js** - 数据配置（无依赖）
2. **storage.js** - 存储管理（无依赖）
3. **base.css + notebook.css + layout.css** - 基础样式
4. **icons.css** - CSS图标
5. **game.js** - 核心逻辑（依赖data.js、storage.js）
6. **ui.js** - 界面渲染（依赖game.js）
7. **components.css + animations.css** - 组件和动画
8. **main.js** - 入口整合
9. **index.html** - HTML结构
